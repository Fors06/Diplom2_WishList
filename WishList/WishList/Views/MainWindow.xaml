<Window x:Class="WishList.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WishList"
        mc:Ignorable="d"
        Title="IT Company - Авторизация"
        Height="650"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding EnterKeyCommand}" />
    </Window.InputBindings>

    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="15"
            Margin="20"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="15,15,0,0" MouseDown="TitleBar_MouseDown">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{Binding CurrentFormTitle}"
                              Foreground="{StaticResource TextBrush}"
                              FontSize="18"
                              FontWeight="Bold"
                              VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Style="{StaticResource CloseButtonStyle}"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <!-- Контент с анимацией перехода -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentForm}">
                <ContentControl.Resources>
                    <!-- Анимация появления -->
                    <Storyboard x:Key="FadeInAnimation">
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                       From="0" To="1" Duration="0:0:0.3"/>
                    </Storyboard>
                </ContentControl.Resources>
            </ContentControl>

            <!-- Футер -->
            <Border Grid.Row="2" Background="{StaticResource SecondaryBrush}" CornerRadius="0,0,15,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                    <TextBlock Text="{Binding FooterText}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              FontSize="10"/>
                    <TextBlock Text="{Binding SwitchFormText}"
                              Foreground="{StaticResource AccentBrush}"
                              FontSize="10"
                              FontWeight="SemiBold"
                              Margin="5,0,0,0"
                              Cursor="Hand">
                        <TextBlock.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SwitchFormCommand}"/>
                        </TextBlock.InputBindings>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>